export interface NutritionistResponse {
  name: string;
  criteria: number;
  reason: string | null;
  characteristic: string;
  intake: string | null;
}

export interface ChemistResponse {
  name: string;
  criteria: number;
  reason: string | null;
  characteristic: string;
  intake: string | null;
}
